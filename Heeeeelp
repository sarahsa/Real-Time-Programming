func syncAllElevators(){
  for{
    select{
    case <- time.After(time.Millisecond * 20):


    }
  }
}

func syncAllElevators(){
  for{
    select{
      case msg :=<- networkChannel:
        handleMsg(msg)
      case <- time.After(time.Milllisecond * 20):
          networkChannel <- stateInfo
    }
  }
}
